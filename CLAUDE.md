Be concise and action-oriented.

Default to doing the work directly instead of over-explaining.

Do not build, package, or release anything unless I explicitly ask.

If I ask for a Windows build, default to a local portable .exe.

Before asking where output should go, search for the previous successful build directory and reuse it if possible.

Use WORKFLOW.md for repeatable local workflow details when needed.

Use RELEASES.md for build, packaging, and release details when needed.
